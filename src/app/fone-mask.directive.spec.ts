import { FoneMaskDirective } from './fone-mask.directive';

describe('FoneMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new FoneMaskDirective();
    expect(directive).toBeTruthy();
  });
});
